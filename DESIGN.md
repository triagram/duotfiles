# DESIGN — 设计原则、框架与关键细节

> 这份文档回答 **「为什么是这样」**。
> 「这是什么、怎么用」在 [README.zh-CN.md](README.zh-CN.md)（英文版 [README.md](README.md)）；
> 「AI 在这个仓库里的工作规矩」在 [AGENTS.md](AGENTS.md)；
> 「某个软件的具体判断」在 [notes/](notes/)。

---

## 一、设计原则

### 1. 分层是决策的产物，不是前提

配置**先按平台各存一份完整的**，只有当你明确判断「这部分两边应该一致」之后，
它才被提到共享层。

反过来做（先设计 `common/` + 平台增量，再往里填）等于在没看到真实差异之前
就替自己做了融合决策，而这个决策的依据还不存在。

> **体现**：`dof adopt` 永远写进 `tools/<软件>/<平台>/`，从不自动写 `common/`。
> 往 `common/` 移文件是纯手工动作，因为那就是「我决定了」的意思。

### 2. 默认 link；失败可检测优于失败静默

| | link 模式的失败 | copy 模式的失败 |
|---|---|---|
| 什么情况 | 链接被应用的原子替换顶掉 | 你改完忘了 `dof push` |
| 多久一次 | 只在特定软件上，通常一次性发现 | 每一次手改目标文件都可能 |
| 能否察觉 | ✅ `dof status` 自动报 `UNLINKED` | ❌ git 一片干净，毫无迹象 |
| 后果 | 改动还在，只是没进仓库 | 下次 `dof pull` 覆盖，**永久丢失** |

一个是「可检测、少数、一次性」的问题，另一个是「不可检测、高频、会丢数据」的问题。
所以默认 link，只对**被证实**会顶掉链接、或会往目录里生成产物的软件用 copy。

link 还顺带满足了核心诉求：改完配置 `git status` 立刻变红，**不存在「忘记同步」这件事**。

### 3. 只链单个文件，从不链目录

部署时父目录一律用 `mkdir -p` 建成**真目录**，只有叶子文件是符号链接。

如果整个目录被链接到仓库（GNU Stow 的默认 folding 行为就是这样），那么应用往
`~/.claude/projects/` 写的每一条会话记录都会**直接落进你的 git 仓库**，
一个 `git add -A` 就推到 GitHub 了。这个失败模式太贵，从结构上排除掉。

### 4. 白名单优先于黑名单

对于「配置和本机状态混在同一个目录」的软件（`~/.claude` 是典型），
黑名单意味着：软件将来新增一个状态目录，它就会悄悄进你的仓库。白名单不会。

> **体现**：`tools/<软件>/.dofkeep`。存在时 `dof adopt` 只收编匹配的路径。

### 5. 软件自己的机制优先于本仓库的机制

凡是软件自带的能力，一律用它的，不要在外面再造一层：

| 需求 | 用软件自己的 | 而不是 |
|---|---|---|
| Rime 用户词库跨机合并 | Rime 的 `sync_dir` + `installation_id` | git（二进制，只会冲突） |
| kitty 按平台加载 | `include ${KITTY_OS}.conf` | 模板引擎 |
| Ghostty 引入平台片段 | `config-file = ?platform-macos.conf` | 模板引擎 |
| tmux 引入平台片段 | `source-file -q` / `if-shell` | 模板引擎 |

**这是本仓库不需要模板引擎的根本原因**：需要区分平台的那几个软件，
自己就有 include 机制。

### 6. 仓库里只放「我自己写的东西」

上游方案（rime-ice、插件仓库等）**部署时现拉**，不进仓库、不做 submodule。

理由是核心诉求：你要能一眼看清自己改了什么。几百个上游文件混进来之后，
`git log` 和 `git diff` 就废了 —— 而这两个恰恰是你做融合决策的主要工具。

代价是版本不锁定。需要的话在 `notes/` 里记一个 commit hash 补偿。

### 7. git 只负责传输与历史；融合是显式的人工动作

两台机器改各自的平台目录 → **路径不同 → 根本不产生 git 冲突**。

平台差异被表达成「目录并存」而不是「同一文件的两个版本」，于是
「什么时候把两边收敛」变成一个你主动发起的动作，而不是 `git pull` 时被迫处理的意外。

### 8. 没有「全局」命令

所有命令都以单个软件为单位。不提供 `dof pull --all`，不提供整机 bootstrap。

不是做不到，是**不想要**：新机器上你往往只需要其中三个软件，
全量部署会把你没想清楚的东西也铺上去。

---

## 二、总体框架

### 目录结构

```
duotfiles/
├── manifest              每个软件一行：叫什么、怎么部署、部署到哪
├── bin/dof               部署器（约 380 行 bash，无依赖）
├── tools/
│   └── <软件>/
│       ├── common/       两个平台共用（融合之后才会有东西）
│       ├── linux/        Linux 专属
│       ├── macos/        macOS 专属
│       ├── .dofignore    可选：这些路径不参与搬运（黑名单）
│       └── .dofkeep      可选：只有这些路径能被 adopt 收编（白名单）
├── notes/<软件>.md       决策记录：为什么这里必须分平台
├── AGENTS.md             AI 的工作规矩（CLAUDE.md 是指向它的软链）
├── DESIGN.md             本文件
└── README.md             怎么用
```

### manifest 格式

四列，空白分隔：

```
# 名称     方式   linux 目标                        macos 目标
tmux       link   ~/.config/tmux                    ~/.config/tmux
rime       copy   ~/.local/share/fcitx5/rime        ~/Library/Rime
```

目标写 `-` 表示该平台不部署。**路径不能含空格** —— 本仓库涉及的路径都没有
（Ghostty 在 macOS 上也读 `~/.config/ghostty`，所以我们避开了
`~/Library/Application Support/` 这种带空格的路径）。
用四列纯文本而不是 TOML/YAML，是为了让 bash 一行 `read` 就能解析，不引入任何依赖。

### 三层解析：一个机制表达三种状态

部署时把 `common/` 铺开，再把 `<当前平台>/` 盖上去，**同名文件平台层胜出**。

同一个机制覆盖了配置演化的全部三个阶段，**升级不需要重构目录**：

| 阶段 | 目录里有什么 | 含义 |
|---|---|---|
| 一（起步） | 只有 `linux/`、`macos/` | 完全分离，两边各存一份完整配置 |
| 二（部分收敛） | 三层都有 | `common/` 是共享部分，平台层是增量或覆盖 |
| 三（完全一致） | 只有 `common/` | 确认两边没有差异 |

### 数据流

```
                   ┌──────────── link 模式 ────────────┐
                   │                                    │
   仓库文件 ●══════════ 符号链接 ══════════● ~/.config/…/xxx
     ▲             │   （只有一份内容）      │
     │             └────────────────────────┘
     │  git commit / push                    改配置 → 仓库文件立即变化
     │                                        （无需任何同步动作）
     │
     │             ┌──────────── copy 模式 ────────────┐
     │             │                                    │
   仓库文件 ●───── dof pull ──────────────────▶ ● ~/…/xxx
             ●◀──────────────── dof push ──────┘
                   │   （两份独立文件）       │
                   └────────────────────────┘
```

跨机器：`git push` / `git pull` 搬运仓库，**两端各自 `dof pull` 部署**。
没有自动同步，没有守护进程 —— 每一次生效都是你按下的。

---

## 三、关键细节

### link / copy 的判据

用 `copy` 只有两个理由：

1. **应用会往配置目录里生成产物** —— Rime 每次部署都写 `build/`
2. **应用用「写临时文件 + rename」保存配置** —— rename 是目录层面的替换，
   会把符号链接本身顶掉，导致静默失联

第 2 条无法预判，只能观察。所以 `dof status` 内建了检测：目标位置本该是链接、
现在是普通文件，就报 `UNLINKED` 并提示两种可能的原因。

> 值得留意的软件：`~/.claude/settings.json`（Claude Code 的 `/config` 会写它）。
> 部署后跑一次 `dof status claude` 确认。

### 文件状态机

`dof status` 对每个受管文件给出六种状态之一：

| 状态 | 含义 | 处理 |
|---|---|---|
| `ok` | 链接正确 / 内容一致 | — |
| `missing` | 仓库有，本机没部署 | `dof pull` |
| `drift` | copy 模式下两边内容不同 | `dof diff` 看，然后 `dof push` 或 `dof pull` |
| `UNLINKED` | 本该是链接，却是普通文件 | 见上一节 |
| `MISLINKED` | 是链接，但指向别处 | `dof pull` 修复 |
| `BROKEN` | 链接悬空，源文件没了 | `dof pull` 修复 |

### .dofignore 与 .dofkeep 的分工

| | 作用范围 | 语义 | 用在哪 |
|---|---|---|---|
| `.dofignore` | 所有命令 | 黑名单：这些路径完全不存在于 dof 眼中 | Rime 的 `build/`、`*.userdb/` |
| `.dofkeep` | 只影响 `adopt` / `push` 的收编 | 白名单：只有这些能进仓库 | `~/.claude`、`~/.gemini` |

两者可以叠加。`.dofkeep` 只管「什么能进来」，是因为部署方向（`pull`）的内容
本来就是你自己 commit 进去的，不需要再过滤一次。

### 凭据的三道防线

1. **`.dofkeep`** —— `dof adopt` 根本不会把 `.credentials.json` 复制进仓库
2. **`.gitignore`** —— 即使手工复制进来了，git 也不会跟踪
3. **人工 review** —— `dof adopt` 打印每一个收编的文件，并要求你 `git status` 后再 commit

三道都是「宁可误伤」的方向。被误伤的文件用 `git add -f` 单独放行。

### 覆盖前一律备份

`dof pull` 遇到已存在且不同的目标文件，先 `mv` 成 `xxx.dof-bak-<时间戳>` 再写。
备份文件已在 `.gitignore` 里。清理是手工的 —— 自动删除的东西没法后悔。

### Windows 的扩展点

现在不支持，但没有堵死。要加的话只需两处改动：
`bin/dof` 里 `uname -s` 的 `case` 加一个分支，`manifest` 加第五列。
`tools/<软件>/windows/` 这一层的存在与否由目录决定，不需要改代码。

---

## 四、明确不做的事

| 不做 | 为什么 |
|---|---|
| 装软件、配系统 | 那是包管理器和 Ansible 的活。混进来会让「部署配置」这件事变重 |
| 自动同步 / 文件监听 / 守护进程 | 你要的是「手动在多端同步」。自动同步会在你没准备好的时候把半成品推到另一台机器 |
| 模板引擎 | 需要平台差异的软件自己有 include 机制（原则 5） |
| 管理用户词库、凭据、会话记录 | 前者有专用同步机制，后两者根本不该离开本机 |
| 整机一键 bootstrap | 见原则 8 |
| 自动融合两个平台的配置 | 融合是判断，不是操作。工具负责把差异摆到你面前，判断由你做 |

---

## 五、演进路线

**阶段一 · 现在** —— 两台机器上分别 `dof adopt`，把现有配置原样收进 `linux/` 和 `macos/`。
不建 `common/`，不写融合规则。目标是先让所有东西进仓库、有历史。

**阶段二 · 两套都在了** —— 让 AI 逐个软件 diff 两个平台层。
预期结论：`claude`、`agy` 大概率零差异；`tmux` 可能只差剪贴板那一两行；
真正有差异的只有终端模拟器。**很多软件根本不需要「融合」这个动作。**

**阶段三 · 按需收敛** —— 只对「差异确实小、且你确实烦了改两遍」的软件动手：
把共同部分移进 `common/`，剩下的用软件自带的 include 机制串起来，
并在 `notes/<软件>.md` 里记下「为什么剩下这几行必须分平台」。

**以后可能加的**（都等到真的烦了再说）：
`dof upstream`（自动拉 rime-ice 这类上游）、
`/merge-config` 这个 Claude Code 自定义命令（把「比对两个平台层」固化成一句话）。
两者都不该现在做 —— 需求还没被验证过。
